# Write your MySQL query statement below
select temp.project_id,round(AVG(temp.experience_years),2) as average_years from (select project_id,experience_years from Project join Employee on Project.employee_id=Employee.employee_id) as temp group by project_id;